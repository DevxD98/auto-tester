# Copy this to apps/web/.env.local and fill your key to enable free AI
# These env vars are read by the Next.js app at runtime (server routes)
HUGGINGFACE_API_KEY=
HF_MODEL=bigscience/bloomz-560m
HF_TEXT_MODEL=google/flan-t5-base
